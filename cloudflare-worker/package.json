{
  "name": "codetrail-cloudflare-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker for CodeTrail — exchanges GitHub OAuth codes for access tokens",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240605.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.65.0"
  }
}
