# Dependencies
node_modules/

# Expo
.expo/
.expo-shared/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.apk
*.aab
*.ipa
*.app

# Metro
.metro-health-check*

# Debug
npm-debug.*
yarn-debug.*
yarn-error.*
*.log
.env*.local
.env
!.env.example

# macOS
.DS_Store
.AppleDouble
.LSOverride
*.pem

# Windows
Thumbs.db
ehthumbs.db
[Dd]esktop.ini
$RECYCLE.BIN/

# IDEs
.vscode/
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# Test / coverage
coverage/
*.lcov

# TypeScript build artifacts
*.tsbuildinfo

# Generated native folders (created by `npx expo prebuild`)
/ios
/android

# Firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# Expo example (residual from template, regenerate with `npm run reset-project`)
example

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
# Stale wrangler config that may be auto-generated when wrangler is
# accidentally run from the project root (it treats the Expo web build
# as a static-assets worker and overwrites our module worker). The
# real config lives in cloudflare-worker/wrangler.toml and IS tracked.
/wrangler.jsonc
